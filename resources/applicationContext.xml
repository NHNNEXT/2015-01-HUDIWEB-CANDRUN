<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd">
	<context:property-placeholder location="classpath:/application-properties.xml" />

	<context:annotation-config></context:annotation-config>

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
		p:driverClassName="${database.driverClassName}" p:url="${database.url}"
		p:username="${database.username}" p:password="${database.password}"></bean>

	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host" value="smtp.gmail.com" />
		<property name="port" value="587" />
		<property name="username" value="gubagiserver@gmail.com" />
		<property name="password" value="candy123$%" />
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.ssl.trust">smtp.gmail.com</prop>
				<prop key="mail.smtp.starttls.enable">true</prop>
				<prop key="mail.smtp.auth">true</prop>
			</props>
		</property>
	</bean>

	<bean id="mailMessage" class="org.springframework.mail.SimpleMailMessage">
		<property name="from" value="gubagiserver@gmail.com" />
		<property name="subject" value="구바기 회원인증 이메일입니다." />
		<property name="text">
			<value>
		<![CDATA[

환영합니다. 친구들을 구박해주세요~~~

아래 링크로 이동하시면 회원가입이 완료됩니다.
		]]>
			</value>
		</property>
	</bean>

	<bean id="mailService" class="candrun.mail.CertifMailService">
		<property name="mailMessage" ref="mailMessage" />
		<property name="mailSender" ref="mailSender" />
	</bean>

	<!-- 어노테이션 방식으로 바꾸자. -->
	<bean id="userDao" class="candrun.dao.UserDAO" p:dataSource-ref="dataSource" />
	<bean id="goalDao" class="candrun.dao.GoalDAO" p:dataSource-ref="dataSource" />
	<bean id="taskDao" class="candrun.dao.TaskDAO" p:dataSource-ref="dataSource" />

</beans>
